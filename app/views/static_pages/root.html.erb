<script type="text/javascript">
  <% if logged_in? %>
    window.session = <%= render(
      "api/sessions/session.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>
  window.welcomePath = <%= asset_path('disclone_marked.png') %>
</script>

<main id="root" />

